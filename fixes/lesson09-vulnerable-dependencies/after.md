# After Fix

`node-serialize` was removed from the public request path and replaced with safe JSON parsing. Other vulnerable dependencies should be upgraded or replaced while preserving JWT verification.
